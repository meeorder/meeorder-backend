services:
  web:
    build: .
    ports:
      - "1234:80"
    links:
      - "mongo:database"
    environment:
      MONGO_URI: mongodb://database/meeorder

  mongo:
    image: mongo
    ports:
      - "27017:27017"